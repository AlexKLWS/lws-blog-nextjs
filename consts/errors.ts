const noName = {
  id: 'no-name',
  description: 'No title specified!',
}

const noSubtitle = {
  id: 'no-subtitle',
  description: 'No subtitle specified!',
}

const noIcon = {
  id: 'no-icon',
  description: 'No icon!',
}

export const materialErrors = {
  noName,
  noSubtitle,
  noIcon,
}

const noURL = {
  id: 'no-ext-material-url',
  description: 'No URL to external material!',
}

export const extMaterialEditorErrors = {
  noURL,
}

const noArticleText = {
  id: 'no-article-text',
  description: 'No article content!',
}

export const articleEditorErrors = {
  noArticleText,
}

const noDefaultZoom = {
  id: 'no-default-zoom',
  description: 'No default zoom specified!',
}

const noInfo = {
  id: 'no-info',
  description: 'No info specified!',
}

const noDefaultCenter = {
  id: 'no-default-center',
  description: 'No default center coordinates specified!',
}

const noLocations = {
  id: 'no-locations',
  description: 'At least one location should be added!',
}

const noLocationType = {
  id: 'no-location-type',
  description: 'One of locations has no type specified!',
}

const noLocationCoordinates = {
  id: 'no-location-coordinates',
  description: 'One of locations has no coordinates specified!',
}

const noLocationAddress = {
  id: 'no-location-address',
  description: 'One of locations has no address specified!',
}

const noLocationTitle = {
  id: 'no-location-title',
  description: 'One of locations has no title specified!',
}

const noLocationDescription = {
  id: 'no-location-description',
  description: 'One of locations has no description specified!',
}

const noLocationImageUrl = {
  id: 'no-location-image-url',
  description: 'One of locations has no image URL specified!',
}

const noLatitude = {
  id: 'no-latitude',
  description: 'Coordinates property has latitude missing!',
}

const noLongitude = {
  id: 'no-longitude',
  description: 'Coordinates property has longitude missing!',
}

export const guideEditorErrors = {
  noDefaultZoom,
  noInfo,
  noDefaultCenter,
  noLocations,
  noLocationType,
  noLocationCoordinates,
  noLocationAddress,
  noLocationTitle,
  noLocationDescription,
  noLocationImageUrl,
  noLatitude,
  noLongitude,
}
